import * as React from 'react'
import styled from 'styled-components'

const DotsDefault = styled.nav`
    @keyframes identifier {
        
    }
`

const BackDots = () => {
    return (
        <NavContainer>

        </NavContainer>
    )
}

export default BackDots;